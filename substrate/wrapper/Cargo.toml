[package]
name = "gradle-wrapper"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "gradlew"
path = "src/main.rs"

[dependencies]
sha2 = "0.10"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
zip = "2"

[dev-dependencies]
tempfile = "3"
